import { LoginContainer } from '@/containers'
import { Layout } from '@/components'

const login = () => {
  return (
    <Layout title={'Login'}>
        <LoginContainer />
    </Layout>

  )
}

export default login
