import React from 'react'
import { Spinner, Loader } from './styles'

const Loading = () => {
  return (
        <Spinner>
            <Loader></Loader>
        </Spinner>
  )
}

export default Loading
